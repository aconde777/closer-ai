{
  "name": "closer-ai",
  "version": "1.0.0",
  "description": "AI voice sales roleplay trainer for high-ticket closers",
  "main": "server.js",
  "scripts": {
    "build": "esbuild node_modules/@vapi-ai/web/dist/vapi.js --bundle --platform=browser --global-name=Vapi --outfile=public/vapi.bundle.js",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@vapi-ai/web": "^2.3.0",
    "esbuild": "^0.20.0"
  }
}
